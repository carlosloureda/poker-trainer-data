{
  "name": "poker-trainer",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:app\"",
    "dev:api": "tsx api/local-server.ts",
    "dev:app": "cd app && npm run dev",
    "build": "cd app && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^16.4.5",
    "tsx": "^4.19.1"
  }
}
